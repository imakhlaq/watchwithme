server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql:///watchwithme
    username: postgres
    password: sBCA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.springframework.web.socket.config.annotation: ERROR